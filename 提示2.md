优化 Purrdoku 微信小游戏的「提示」系统。

本次在现有提示推理逻辑基础上增加两种提示展示模式：

1. **交互式推理教学**
2. **自动播放动画**

用户可以在「设置」中选择。

默认：

**交互式推理教学**

注意：本次主要修改提示的展示方式、阶段控制和设置项。如果现有 Hint Solver / 推理算法正确，不要重写底层提示算法。

---

# 一、设置中增加「提示方式」

在游戏「设置」中增加：

### 提示方式

提供两个选项：

**交互式推理教学（推荐）**

> 一步一步查看推理过程

**自动播放动画**

> 自动演示完整推理过程

默认：

`交互式推理教学`

保存用户选择到现有本地设置/Storage 中，下次进入游戏继续使用用户上一次选择。

如果老用户本地没有这个配置，则使用默认值：

`interactive`

建议内部枚举：

```ts
hintMode: 'interactive' | 'auto';
```

不要根据显示中文文案判断模式。

---

# 二、交互式推理教学

这是默认模式，也是重点。

核心原则：

**不要自动快速播放整个提示。**

让玩家自己控制推理节奏。

提示打开以后，例如一个 4 阶段推理：

### 第 1 步

文字：

> 先看第5列

棋盘只高亮第5列/相关候选格。

动画完成以后：

**停在这里。**

不要自动进入下一阶段。

显示：

`下一步 ›`

---

### 第 2 步

用户点击「下一步」。

展示：

> 第5列的猫只可能在这些位置。

对应候选格进行描边、发光或呼吸。

动画完成以后再次：

**停住。**

等待用户点击：

`下一步 ›`

---

### 第 3 步

继续：

> 而这些位置全部属于玫粉区域。

同时强调玫粉区域以及与上一阶段的关系。

继续停住。

等待：

`下一步 ›`

---

### 第 4 步

展示最终结论：

> 所以玫粉区域里的其他位置都不能放猫。

需要排除的位置依次出现 `×`。

最终结果全部展示完成以后：

**永久保持最终状态，不再自动变化。**

底部操作变为：

`应用这些 ×`

`先自己想想`

如果结果是确定猫，则：

`把猫放这里`

`先自己想想`

---

# 三、交互式模式必须支持「上一步」

除了：

`下一步 ›`

建议同时支持：

`‹ 上一步`

但第一阶段不显示/禁用「上一步」。

这样玩家如果某一步没看懂，可以返回重新看。

例如：

`‹ 上一步      2 / 4      下一步 ›`

阶段指示建议显示：

`1 / 4`

`2 / 4`

`3 / 4`

`4 / 4`

不要做复杂进度条。

保持轻量。

---

# 四、交互式模式的动画

交互模式不是完全取消动画。

每次玩家点击「下一步」以后：

使用约：

`0.25～0.4 秒`

完成：

- 高亮
- 描边
- 淡入
- 呼吸
- × 出现
- 猫出现

动画完成以后立即停住。

因为阅读时间由玩家自己决定，所以：

**交互模式不需要设置自动进入下一阶段的 timer。**

这是与自动播放模式最大的区别。

---

# 五、自动播放动画模式

用户如果在设置中选择：

**自动播放动画**

则自动演示完整推理过程。

但是目前：

- 4 阶段：0.55 秒/阶段
- 3 阶段：0.7 秒/阶段

明显太快。

需要重新调整。

---

# 六、自动播放新的节奏

### 4 阶段提示

每个阶段停留：

**1.2～1.5 秒**

推荐默认：

`1.35s`

---

### 3 阶段提示

每个阶段停留：

**1.3～1.6 秒**

推荐默认：

`1.45s`

---

阶段本身的视觉过渡：

**0.25～0.35 秒**

注意区分：

`transitionDuration`

和：

`readingDuration`

不要把两者混成一个 timer。

动画可以比较快，但动画结束以后必须留出足够时间让玩家：

**看清 → 阅读 → 理解。**

---

# 七、自动播放最终结果

最后一步如果有多个：

- ×
- 排除格
- 候选格
- 其他推理结果

不要同时出现。

依次展示。

建议：

单个结果淡入：

`0.25～0.30 秒`

相邻结果间隔：

`0.10～0.15 秒`

让玩家明显看到：

> 一个一个位置被推理排除。

如果最终确定的是猫：

使用：

`fade + scale`

例如：

`scale 0.9 → 1.05 → 1.0`

整体约：

`0.3～0.4 秒`

不要使用夸张弹跳。

---

# 八、自动播放允许玩家主动加速

自动播放模式下：

如果玩家已经理解当前阶段，不应该强迫他等待。

允许玩家：

**点击提示棋盘 / 当前提示说明区域**

立即结束当前阶段的等待时间并进入下一阶段。

注意：

不是直接关闭提示。

只是：

`skip current waiting → next stage`

如果正在执行 0.3 秒左右的视觉动画，可以等待动画完成后再进入下一阶段，避免状态错乱。

---

# 九、最终阶段必须停止

无论：

- 交互式推理教学
- 自动播放动画

最终推理完成以后：

**都必须停止。**

不要：

- 自动关闭弹窗
- 自动恢复棋盘
- 循环播放
- 自动进入下一条提示
- 自动应用结果

最终状态一直保持，直到用户主动选择：

`应用这些 ×`

或：

`把猫放这里`

或：

`先自己想想`

或关闭弹窗。

---

# 十、统一 Hint Step 模型

不要分别写两套提示逻辑。

建议把一个提示转换为统一的：

```ts
HintStep[]
```

概念例如：

```ts
interface HintStep {
	title?: string;
	description: string;
	focusCells?: Cell[];
	highlightRow?: number;
	highlightColumn?: number;
	highlightRegion?: string;
	resultCells?: Cell[];
	action?: 'focus' | 'highlight' | 'eliminate' | 'place-cat';
}
```

具体字段根据现有项目结构设计，不要求机械照抄。

关键思想：

**Solver 负责算出为什么。**

**HintStep 负责描述怎么一步一步讲给玩家听。**

然后：

交互模式：

`HintStep 1 → 玩家点击 → HintStep 2 → 玩家点击 → ...`

自动模式：

`HintStep 1 → timer → HintStep 2 → timer → ...`

这样两种模式共用：

- 推理结果
- HintStep
- 文案
- 棋盘高亮
- 动画

区别仅仅是：

**谁来触发 nextStep()。**

---

# 十一、两种模式的关系

最终架构应该类似：

```text
                Hint Solver
                     ↓
                 HintResult
                     ↓
                 HintStep[]
                     ↓
           Hint Presentation
              ↙           ↘
     Interactive           Auto
          ↓                 ↓
   用户 nextStep()       timer nextStep()
```

不要维护两套独立提示系统。

---

# 十二、模式切换

设置中切换模式后：

下一次打开提示立即使用新模式。

如果用户正在看提示时进入设置并修改模式，根据现有页面架构选择最简单可靠方案，不需要强行实时切换当前已经打开的提示。

下一次 Hint Session 生效即可。

---

# 十三、交互模式 UI

不要把提示弹窗做得像教程页面。

保持现在简洁的白色卡片设计。

棋盘仍然是视觉主体。

建议布局：

```text
这只猫只能在这里

第3列还缺一只猫。
排除不能放猫的位置后，
只剩这个格子。

        [提示棋盘]

      ‹ 上一步   2 / 3   下一步 ›

--------------------------------

          应用这些 ×

           先自己想想
```

如果当前还没有到最终阶段：

可以暂时隐藏「应用」按钮。

只有最终推理结论展示出来以后，再显示：

`应用这些 ×`

或者：

`把猫放这里`

这样用户会自然地跟着整个推理走完。

---

# 十四、可以跳到结果，但不要喧宾夺主

考虑熟练用户在交互模式下可能不想一步一步点。

可以提供一个非常弱的文字入口：

`直接看结果`

但视觉权重要明显低于：

`下一步`

如果现有弹窗空间不合适，可以暂时不实现。

不要为了这个功能破坏当前 UI。

---

# 十五、提示过程不要阻塞关闭

任何阶段：

右上角 `×` 都可以关闭提示。

「先自己想想」可以保留。

关闭后：

**不能修改真实棋盘。**

提示过程中所有：

- 临时 ×
- 临时猫
- 高亮
- 描边
- 候选状态

都只是 Hint Preview。

只有点击：

`应用这些 ×`

或者：

`把猫放这里`

才真正修改游戏状态。

这一点必须严格保证。

---

# 十六、体验目标

### 新玩家

默认：

`交互式推理教学`

体验：

```text
卡住
 ↓
点击提示
 ↓
“先看第5列”
 ↓
自己观察
 ↓
下一步
 ↓
“猫只能在这些位置”
 ↓
自己观察
 ↓
下一步
 ↓
“这些位置都属于玫粉区域”
 ↓
理解关系
 ↓
下一步
 ↓
其他位置依次出现 ×
 ↓
“哦！原来是这样”
 ↓
应用这些 ×
```

提示真正起到：

**教会玩家推理方法**

的作用。

### 熟练玩家

可以：

设置 → 提示方式 → 自动播放动画

点击提示后自动完成：

`观察 → 推理 → 结论`

如果嫌慢：

点击提示区域即可加速到下一阶段。

---

# 十七、不要改 Solver

实施前先检查当前提示代码。

如果现有推理算法正确：

**禁止因为这次 UI/交互优化而重写 Solver。**

优先修改：

- Hint Presentation
- Hint Modal
- Hint Step 状态
- 动画控制
- 设置
- 本地持久化

尽量降低对游戏核心逻辑的影响。

---

# 十八、先分析再修改

先检查当前代码，确认：

1. 当前 3 阶段/4 阶段是如何实现的
2. timer 在哪里控制
3. HintResult 当前数据结构
4. UI 如何根据 stage 渲染
5. 最终结果动画如何实现
6. 设置数据如何持久化
7. 是否能够复用现有 stage 数据实现两种模式

先给我一个简短的实施方案和预计修改文件。

确认没有必要重写 Hint Solver 后，再开始修改。

完成以后重点测试：

- 3 阶段交互模式
- 4 阶段交互模式
- 上一步/下一步
- 最终应用
- 中途关闭不修改棋盘
- 3 阶段自动模式
- 4 阶段自动模式
- 点击加速
- 设置持久化
- 老用户没有 hintMode 配置时默认 interactive
- 不同棋盘尺寸
- 不同提示类型
- 连续多次打开提示

最终验收标准：

> **默认情况下，玩家可以按照自己的阅读和思考速度，一步一步理解这次提示为什么成立；如果不想手动看教学，也可以在设置中切换为自动播放。**
