{
  "name": "purrdoku-minigame",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node tools/build.mjs",
    "check:types": "tsc -p tsconfig.json --noEmit",
    "test": "node tools/run-tests.mjs",
    "gen:static": "node tools/gen-static.mjs",
    "verify": "npm run check:types && npm run build && npm run test"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "esbuild": "^0.25.9",
    "typescript": "~5.8.3"
  }
}
