请优化 Purrdoku 微信小游戏中的「提示」系统。

## 一、目标

当前提示功能已经能够根据棋盘状态给出正确的推理，并展示：

- 提示标题
- 推理说明
- 推理棋盘
- 推理依据
- 应用后的变化
- 「应用」
- 「先自己想想」

现有底层推理逻辑如果正确，**不要重写提示算法**。

本次重点优化：

**让第一次玩的普通用户，不需要理解专业术语，也能一眼看懂“为什么这里可以打 × / 为什么这里必须放猫”。**

提示不是单纯告诉玩家答案，而应该：

> 像一个很会教人的玩家，在棋盘上一步一步演示“我是怎么看出来的”。

最终目标：

**少读文字，多看棋盘；先看哪里 → 为什么 → 得出什么结论。**

---

# 二、不要使用过于专业的推理术语

当前类似：

- 这个区域与直线互相锁定
- 推理范围
- 推理依据
- 白框
- ◆
- 占用两者

这些表达偏算法/说明书语言。

内部算法、代码仍然可以使用专业名称，但玩家 UI 文案应该转换成自然语言。

例如：

原：

> 这个区域与直线互相锁定

改成：

> 第5列的猫，一定在玫粉区域里

原：

> 第5列剩下的位置全部位于玫粉区域。这只猫会占用两者，因此玫粉区域中其余白框位置都不能再放猫。

改成：

> 第5列只剩这些位置能放猫，而它们都在玫粉区域。
> 所以玫粉区域的猫一定会出现在第5列，其他位置都可以排除。

玩家应该先看到**具体结论**，而不是先学习一个推理术语。

---

# 三、提示统一采用三段式思路

所有提示尽量转换成：

### 1. 先看哪里

告诉玩家：

> 先看第5列

或者：

> 先看玫粉区域

或者：

> 先看第3列

同时在提示棋盘中高亮对应：

- 行
- 列
- 区域
- 候选格

### 2. 为什么

使用一句非常短的自然语言解释。

例如：

> 第5列的猫只可能出现在这些格子，而这些格子全部属于玫粉区域。

### 3. 所以呢

明确告诉玩家得到什么结果：

> 所以玫粉区域里的其他位置都不能放猫。

然后通过棋盘动画出现 ×。

整个提示遵循：

**观察 → 推理 → 结论**

不要一次性把所有信息塞给玩家。

---

# 四、不同提示类型使用不同的自然语言

## 类型 A：唯一位置

不要使用复杂术语。

标题：

> 这只猫只能在这里

说明：

> 第3列还缺一只猫。
> 排除不能放猫的位置后，只剩这个格子。

视觉上：

先高亮第3列 → 再高亮唯一候选格 → 候选格轻微呼吸 → 显示猫。

按钮：

> 把猫放这里

而不是：

> 应用

---

## 类型 B：行/列与区域锁定

例如第5列的所有候选位置都落在玫粉区域。

标题：

> 第5列的猫，一定在玫粉区域里

说明：

> 第5列只剩这些位置能放猫，而它们都在玫粉区域。
> 所以玫粉区域里的其他位置都可以排除。

视觉：

第一阶段：

高亮第5列候选格。

第二阶段：

强调这些候选格都属于玫粉区域。

第三阶段：

玫粉区域其他可排除格依次出现 ×。

按钮：

> 应用这些 ×

---

## 类型 C：区域与行/列反向锁定

例如某个区域剩余候选全部位于第4列。

标题：

> 这个区域的猫，一定在第4列

说明：

> 这个区域能放猫的位置都在第4列。
> 所以第4列其他区域的位置都可以排除。

视觉过程同样采用：

区域 → 第4列 → 排除位置。

---

## 类型 D：无论放在哪都会产生相邻冲突

原来的：

> 不论猫在哪，都会相邻

可以保留这个思路，但表达得更直观。

标题：

> 这些位置都不能放猫

说明：

> 第4行还缺一只猫。
> 不管猫最后放在哪个候选位置，都会和这些格子相邻。
> 所以这些位置都可以排除。

视觉：

先高亮第4行的候选位置。

然后高亮“无论猫在哪都会受到影响”的格子。

最后这些格子依次出现 ×。

按钮：

> 应用这些 ×

---

## 类型 E：已有猫导致排除

标题：

> 这只猫已经排除了这些位置

说明：

> 猫的周围不能再有猫，所以这些相邻位置都可以排除。

视觉：

先让已有猫轻微放大/呼吸。

然后它周围受影响的格子依次出现 ×。

按钮：

> 应用这些 ×

---

# 五、减少符号系统

当前提示棋盘中存在：

- 猫
- ×
- ◆
- 白框
- 彩色区域
- 推理范围

认知元素偏多。

尽量取消：

> ◆：推理依据

不要要求玩家额外学习一个 ◆ 的含义。

改用视觉表现：

### 普通格

保持原棋盘状态。

### 当前需要观察的格子

使用：

- 描边
- 轻微发光
- 呼吸动画
- 适当提高亮度

### 与当前推理无关的格子

适当降低透明度。

### 本次新排除的位置

动画出现 ×。

### 本次确定的位置

猫咪动画出现。

让玩家通过视觉自然理解：

> 哪些是“正在看的”
> 哪些是“得到的结果”

而不是通过图例学习。

---

# 六、弱化或取消底部技术说明

当前类似：

> 推理范围：第5列 · 玫粉区域
> 白框：应用后的变化 · ◆：推理依据

这些内容偏开发者/数独工具风格。

建议取消，或者大幅弱化。

核心信息直接通过棋盘表达。

如果保留辅助说明，只保留非常简单的一句，例如：

> 高亮位置是这一步需要观察的地方

但如果视觉已经足够清楚，则不需要显示。

---

# 七、提示不要直接展示最终状态

这是本次最重要的改动之一。

现在打开提示后，基本直接看到完整推理结果。

改成一个短的分阶段演示。

例如：

### 阶段 1

弹窗打开。

文字：

> 先看第5列

第5列轻微高亮。

### 阶段 2

约 500~800ms 后：

候选位置进一步高亮。

文字：

> 第5列的猫只可能在这些位置。

### 阶段 3

强调玫粉区域。

文字：

> 而这些位置全部属于玫粉区域。

### 阶段 4

需要排除的位置依次出现 ×。

文字：

> 所以玫粉区域里的其他位置都不能放猫。

不要让动画拖沓。

整个演示控制在大约 1.5~2.5 秒完成。

玩家应该感觉：

**棋盘正在给我演示思路。**

而不是：

**弹出了一篇教程让我阅读。**

---

# 八、动画要求

动画保持 Purrdoku 当前：

- 轻
- 柔和
- 精致
- 治愈
- 不夸张

可以使用：

- 呼吸
- 淡入
- 描边
- 轻微缩放
- stagger 依次出现
- 柔和高亮

不要：

- 强烈闪烁
- 快速抖动
- 大幅弹跳
- 复杂粒子特效

推理依据出现时可以：

`scale 1.0 → 1.04 → 1.0`

最终 × 可以按照推理方向依次出现。

确定猫的位置时，猫咪可以：

轻微缩放 + 淡入 + 很小的弹性效果。

---

# 九、按钮文案动态化

不要所有提示都统一叫：

> 应用

根据推理结果动态显示。

如果结果是添加 ×：

> 应用这些 ×

如果结果是确定猫：

> 把猫放这里

如果同时包含多个操作：

> 应用这一步

第二按钮继续保留：

> 先自己想想

这个交互不要删除。

---

# 十、「应用」的行为

点击应用后：

只应用**当前提示明确推导出来的变化**。

例如：

- 添加本次推导出的 ×
- 或放置本次确定的猫

不要顺便执行下一步推理。

应用完成后关闭弹窗。

回到主棋盘后，对刚刚变化的位置做一次非常轻微的高亮反馈，让玩家知道：

> 刚才提示应用的是这里。

---

# 十一、提示应该教学，而不是作弊

提示系统的核心产品原则：

**提示 = 教玩家走下一步。**

不是：

**提示 = AI 帮玩家把题做掉。**

因此每次提示原则上只解释：

> 当前最值得走的一步。

不要一次给出连续多步答案。

如果应用以后玩家再次点击提示，再根据新的棋盘状态推导下一步。

最终应该形成：

玩家卡住
↓
点击提示
↓
提示让他“先看这里”
↓
棋盘演示为什么
↓
玩家理解
↓
选择「应用」或者「先自己想想」
↓
继续游戏

---

# 十二、技术要求

本次首先阅读现有提示系统代码。

重点确认：

- hint solver
- hint type
- hint result
- 推理数据结构
- hint modal
- mini board renderer
- animation
- applyHint
- 当前棋盘状态与提示棋盘状态的关系

如果底层提示算法目前正确：

**不要为了改 UI 重写 solver。**

优先把：

> 算法产生的 HintResult

转换成：

> 玩家能理解的 HintPresentation

建议把“推理逻辑”和“展示逻辑”分离。

例如概念上：

`HintResult`

负责：

- reasoningType
- sourceCells
- affectedCells
- resultCells
- catCell
- xCells

展示层再根据这些数据生成：

- title
- description
- focus area
- animation steps
- CTA 文案

不要把大量中文文案和动画逻辑塞进 solver。

---

# 十三、兼容不同棋盘

提示系统不能只针对当前 6×6 写死。

需要兼容项目现有：

- 不同棋盘尺寸
- 不同区域布局
- 不同难度
- 关卡模式
- 无限模式

行号、列号、区域颜色名称等应该根据实际 HintResult 动态生成。

不要写死：

> 第5列
> 玫粉区域

这些只是示例。

---

# 十四、修改范围

本次只优化：

**「提示」系统。**

不要顺便大改：

- 主棋盘
- 首页
- 关卡系统
- 设置
- 游戏规则
- 其他无关 UI

保持最小影响范围。

---

# 十五、实施方式

不要一上来大规模修改。

第一步先分析现有提示系统，并输出：

1. 当前有哪些 Hint 类型
2. 每种 Hint 的触发条件
3. 当前 HintResult 数据结构
4. 哪些提示已经能正确推导
5. 哪些只是展示问题
6. 哪些地方需要增加展示层数据
7. 动画应该在哪一层实现
8. 推荐修改哪些文件
9. 是否存在影响 solver 的风险

然后给出：

## 《Purrdoku 提示系统优化方案》

先不要修改代码。

等我确认方案以后，再开始实施。

最终判断标准不是“提示信息是否完整”，而是：

> **一个第一次玩 Purrdoku 的普通用户，看到提示棋盘后，能不能在几秒内明白：为什么这些位置可以打 ×，或者为什么这只猫只能放在这里。**
