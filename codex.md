# Purrdoku 微信小游戏接手分析

已按文档完成本轮分析，**没有修改代码、创建分支、commit 或 push**。

整体判断：**小游戏值得继续在现有架构上改进，不需要重写。** 基础规则、纯逻辑引擎和微信适配已经具备可保留的基础；主要短板是触摸事件边界、连续出题的可靠性、反馈动画，以及部分界面和存档细节。

本次以当前源码为准。小游戏现有 **39 项测试全部通过，TypeScript 检查通过**；另检查了 10 道静态题，均满足结构合法、唯一解和逻辑可解。未运行微信真机或完整双端视觉验收，以下不把源码推断当成实测帧率或延迟。

## 1. 两个项目架构

| 层次       | H5                                        | 微信小游戏                                 |
| ---------- | ----------------------------------------- | ------------------------------------------ |
| 技术栈     | React 19、TypeScript、Vite、Zustand       | TypeScript、Canvas 2D、esbuild             |
| 启动和页面 | `main.tsx`、`App.tsx`、`InfiniteMode.tsx` | `main.ts`、`createApp()`                   |
| 渲染       | DOM、CSS、SVG                             | Canvas 绘图和命中区域                      |
| 规则引擎   | `engine/`                                 | `engine/`                                  |
| 游戏状态   | 普通／每日 store、独立无限模式 store      | `game/play.ts` 加 `ui/app.ts` 内的模式状态 |
| 手势       | Pointer Events、指针捕获                  | 微信触摸事件、`GestureMachine`             |
| 出题       | Worker、最多一道预备题、约束匹配备用池    | 主线程同步生成，失败回退静态题             |
| 存储       | localStorage、校验、无限模式备份与恢复    | 微信同步存储、基础校验                     |
| 动画音频   | CSS、SVG、彩纸、Web Audio                 | PNG 精灵图；未接入音频播放                 |
| 测试       | Vitest、Playwright                        | Node 测试，主要覆盖纯逻辑                  |

小游戏已经有合理的 `engine → game → platform/ui` 分层，但 [app.ts](/D:/Github/Purrdoku-wx/src/ui/app.ts) 约 **1353 行**，同时承担页面、输入、存档、出题、统计和绘制，是后续改造的主要集中点。

**建议保留 Canvas 2D 和现有微信入口，逐步拆出职责。**

## 2. H5 → 小游戏功能对应关系

| H5 功能          | 小游戏当前实现                   | 差异                                   | 是否调整   |
| ---------------- | -------------------------------- | -------------------------------------- | ---------- |
| 棋盘生成         | 随机布局、区域生长、多解修补     | 调度、预算和兜底不同                   | 是         |
| 猫的位置算法     | 每行选列、列不重复、相邻行避邻列 | 基本约束一致，不保证同 seed 同题       | 保留并验证 |
| 区域限制         | 非空、四连通、一区一猫           | 基础规则一致                           | 保留       |
| 单击放 X         | 松手后等待 220ms                 | 主逻辑一致                             | 修输入边界 |
| 再次单击取消 X   | `empty ↔ markedX`                | 一致                                   | 保留       |
| 双击放猫         | 同格第二次按下识别，松手执行     | 窗口一致；事件收尾不同                 | 是         |
| 错误放置         | 红 X、累计失误                   | 判定一致；缺错误反馈动画和声音         | 是         |
| 行／列／区域限制 | 正确放猫后自动排除               | 一致                                   | 保留       |
| 八方向不相邻     | 生成、验证和自动标记均实现       | 一致                                   | 保留       |
| 滑动标记         | 起点到当前终点插值               | 不保留完整转弯路径                     | 是         |
| 撤销             | 最近 100 步，恢复棋盘和当前失误  | 核心语义一致                           | 保留       |
| 胜利判断         | 全部答案位置都有猫               | 缺棋盘长度、猫总数防御校验             | 小幅加固   |
| 失败处理         | 错误不结束游戏                   | 一致，没有生命值失败机制               | 保留       |
| 提示             | 解释、预览、应用、纠错、揭示     | 推理框架接近，预览表现有缺口           | 是         |
| 重开             | 每日重置、无限保留累计统计       | 基本一致；完成后的重开边界有问题       | 是         |
| 下一关           | 同步生成并替换当前题             | 缺预生成、失败重试和严格接收条件       | 是         |
| 每日挑战         | 本地日期映射 10 道静态题         | 日期算法一致，题库不同，跨日保存有风险 | 是         |
| 连续闯关         | 成长曲线、尺寸和难度保护         | 与 H5 完整实现仍有差距                 | 是         |
| 设置             | 自动标记、振动、动态效果         | 缺音效／音乐入口，动态开关未生效       | 是         |

当前 H5 首页已经直接进入连续闯关，**没有独立的关卡花园入口**。小游戏这一入口方向是对的，不应因为文档提到“关卡页面”而额外恢复旧页面。

## 3. 游戏规则差异

两版共同规则已经明确：

- 每行、每列、每个颜色区域恰好一只猫。
- 猫之间八方向不能相邻；远距离同一对角线不受限制。
- 放猫直接核对题目答案，不只是检查与现有猫是否冲突。
- 普通 X 可以取消；猫和错误 X 不能通过单击或拖动擦除。
- 再次尝试错误位置仍会增加失误。
- 0 次失误三星，1–2 次两星，其余一星。
- 提示先展示，应用后才改盘；应用作为一步撤销。
- 无限模式的累计失误、提示次数不会因为撤销或重开而洗掉。

主要差异集中在以下方面。

**静态题库不同。** 两边各有 10 道题，但对应位置的区域和答案均不同。因此，同一天虽然计算出相同的题库索引，两端实际题目也不同。小游戏现有题目本身通过了本次校验，不属于坏题；若要统一题库，应保留旧档题面，避免替换后破坏用户进度。

**无限模式的兜底没有遵守完整难度约束。** 小游戏生成失败后调用 `openingLevel()`，它只根据玩家种子选固定静态题，没有再次检查尺寸、难度窗口和近期去重。可能出现后期突然退回 6×6、重复同一道题的情况。H5 的备用池仍按目标约束筛选，找不到合格题就保留进度并报告失败。

**节奏历史丢失了题目用途。** 小游戏通关写入历史时固定使用 `purpose: "normal"`，无法正确保留挑战／放松题身份，影响后续节奏保护。

**部分保护规则不完整。** H5 对放松题、新尺寸适应题还有更严格的推理链限制和稳定性检查；小游戏虽有成长曲线，但不能视为完整移植。

**完成后重开存在状态不一致。** 小游戏无限模式重开会清除 `completed`，却保留 `recorded`。完成后关闭结果弹窗再重开，重新通关可能被 `onWin()` 的已记录判断提前返回，跳过结果弹窗。

相关代码：[board.ts](/D:/Github/Purrdoku-wx/src/engine/board.ts)、[play.ts](/D:/Github/Purrdoku-wx/src/game/play.ts)、[pacer.ts](/D:/Github/Purrdoku-wx/src/engine/pacer.ts)。

## 4. UI 差异

**当前小游戏已经沿用了 H5 的主要配色。** 底色 `#f4f8fc`、主色 `#8073f5`、文字色 `#293b52` 和前九种区域颜色均已对应。README 中“米纸底、柿子红印章”的描述已经落后于源码。

| 部分     | 当前差异                                                                |
| -------- | ----------------------------------------------------------------------- |
| 首页     | 主要文案和结构接近；小游戏首页猫是静态 PNG，H5 是动态 SVG               |
| 游戏顶栏 | 信息基本齐全，但字体层级、图标和状态区构成不同                          |
| 规则条   | H5 有三张小棋盘示意图；小游戏只有文字短条                               |
| 格子     | H5 圆角约 3–5px；小游戏最高 12px，更像独立圆润方块                      |
| 格间距   | H5 约 2–3px；小游戏按格宽的 8% 计算，大格时更宽                         |
| X 标记   | 小游戏线宽随格子增长，整体比 H5 更粗；错误色也不同                      |
| 猫咪     | H5 是矢量猫头；小游戏是另一套四帧精灵图                                 |
| 底部按钮 | H5 使用统一 SVG；小游戏混用字符和 emoji，设备间外观可能不同             |
| 星级     | H5 使用星星；小游戏使用爪印                                             |
| 提示     | 小游戏缺完整行列标签、区域名称和应用结果预览                            |
| 胜利弹窗 | 小游戏内容较简化，缺庆祝过程和完整后续操作                              |
| 设置     | 小游戏只有三项，H5 有五项                                               |
| 宽屏布局 | H5 棋盘最大宽度 430px；小游戏按屏幕和剩余高度计算，缺一致的内容宽度上限 |

提示界面还有一个明确文案问题：**纠正误标时，小小游戏图例仍显示“将标记 ×”，实际操作却是取消 X。**

建议以 H5 当前源码为视觉基准，先统一棋盘比例、规则示意、图标和提示表达，再处理装饰细节。

## 5. 交互和操作手感差异

这是最值得优先改进的部分。

**单击延迟不是小游戏独有的问题。** 两版都在首次松手后等待 220ms，避免单击和双击冲突；快速点击另一格时，也都会先提交上一格单击。因此，不宜直接缩短双击窗口来“提速”。

H5 更完整的反馈包括：

- 普通按钮按下位移 2px、缩放至 0.97。
- 棋盘格按下提亮，格子本身不缩放。
- X 有出现／消失过渡。
- 放猫、标记和错误有不同声音。
- 错误有摇动，正确放猫有关联区域高亮。

小游戏没有统一按压状态；振动主要在双击放猫时触发，缺少上述反馈组合。

发现的输入问题：

| 问题                                 | 具体影响                                           |
| ------------------------------------ | -------------------------------------------------- |
| 棋盘外松手不调用 `end()`／`cancel()` | 活动手势可能残留，后续点击受影响                   |
| `move()`、`end()` 不接收触点 ID      | 无法完整过滤第二根手指的移动和松手                 |
| 松手时不补处理最终坐标               | 最后一次移动事件缺失时，可能漏掉拖动终点           |
| 拖动只记起点和终点                   | 例如先右移再下移，最终变成起终点连线，之前路径丢失 |
| 按钮只按松手位置触发                 | 不要求从该按钮按下，滑入按钮后松手也可能触发       |
| 弹窗和底层共用命中列表               | 弹窗出现后仍可能触发底层按钮                       |
| 拖动预览也调用 `persist()`           | 高频移动触发同步序列化和存储，存在卡顿风险         |

**弹窗穿透已用内存中的微信事件模拟复现：** 打开设置后，点击底层“规则”按钮所在位置，设置弹窗被切换成规则弹窗。这不是仅凭外观推测。

建议保留 `GestureMachine`，修复完整的按下—移动—松手—取消流程，再增加反馈表现。

证据：[小游戏手势](/D:/Github/Purrdoku-wx/src/game/gestures.ts)、[小游戏事件分发](/D:/Github/Purrdoku-wx/src/ui/app.ts:1243)、[H5 手势](/D:/Github/Purrdoku/h5/src/hooks/useGameGesture.ts)。

## 6. 动画差异

| 动画           | H5 当前实现                      | 小游戏当前实现           |
| -------------- | -------------------------------- | ------------------------ |
| 页面进入       | 250ms 位移／透明度过渡           | 直接切换                 |
| 按钮按压       | 150ms 过渡                       | 无统一反馈               |
| X 出现／取消   | 130ms 缩放／透明度               | 直接显示／消失           |
| 放猫           | 280ms 弹出                       | 直接出现                 |
| 猫咪待机       | 6 秒眨眼周期、8 秒摇头周期，错峰 | 四帧、6 FPS，同步循环    |
| 正确反馈       | 450ms 区域闪光、星光             | 无对应效果               |
| 错误反馈       | 200ms 摇动                       | 红 X                     |
| 通关           | 棋盘／猫动画和彩纸               | 立即打开结果弹窗         |
| 弹窗进入       | 220ms 过渡                       | 直接显示                 |
| 动态关闭       | 禁用动画，猫静止                 | 开关保存了，但绘制不读取 |
| 弹窗期间猫动画 | 暂停棋盘猫待机                   | 继续按系统时间播放       |

小游戏注释写“10 FPS”，实际常量为 **6 FPS**。所有猫使用 `Date.now()` 计算同一帧，因此同步眨眼，四帧约每 **0.67 秒**循环一遍，与 H5 偶尔眨眼的节奏差别很大。

另外，H5 当前普通格子没有独立的逐格入场动画，不应把文档中的检查项直接当成已实现效果。

原生实现建议：增加轻量动画状态，用统一时钟驱动格子、X、猫、弹窗和有限数量的庆祝粒子；继续通过 Canvas 绘制，无需模拟 DOM/CSS。

## 7. 资源差异

| 资源       | 核对结果                                                |
| ---------- | ------------------------------------------------------- |
| 首页猫 PNG | 两项目文件 SHA-256 一致，确认已经直接复用               |
| 首页动态猫 | H5 有约 8KB 动态 SVG；小游戏未使用等价动画              |
| 棋盘猫     | 小游戏使用 1254×1254、约 234KB 的四帧 PNG               |
| 旧表情贴纸 | 小游戏仍有约 150KB JPG 和相关模块，当前棋盘绘制已不使用 |
| 图标       | H5 原创 SVG；小游戏 Canvas 图形、字符和 emoji 混合      |
| 音效       | H5 程序合成，没有可直接复制的音频文件                   |
| 背景       | 两版主要靠样式／绘图生成，不需要新增大背景图            |
| 备用题池   | H5 V5 有 607 道；小游戏没有对应的受约束备用池           |

资源管理的主要问题：

- 构建器将 PNG/JPG 内嵌为 data URL，而图片文件仍保留用于加载回退，存在重复携带资源的可能。
- 旧 `catFaces` 状态仍在生成、保存和恢复，但当前绘制函数忽略表情参数。
- 精灵图单帧 627×627，对多数棋盘格偏大，可在真机清晰度验证后缩小。
- 打包忽略列表未明确覆盖 `reference_image`、`.worktrees` 等本地目录，应检查实际上传文件清单。

现有 `game.js` 约 560KiB，**这不等于微信最终上传包大小**。资源来源和授权也不能仅凭文件存在来证明；本轮确认的是技术复用关系。

## 8. 当前小游戏技术债

按影响程度，主要有：

1. **输入生命周期不完整**：越界松手、多指、按钮起止、弹窗命中隔离。
2. **每日存档日期未绑定会话**：`persist()` 使用保存时的 `dailyKey()`。跨午夜继续旧题，会把旧题写入新日期记录。
3. **出题阻塞主线程**：设置“正在印下一张…”后立即同步计算并清除，提示可能没有机会显示。
4. **失败回退绕过难度约束**：固定开场题无法承担后期备用题职责。
5. **存档校验偏弱**：每日记录直接使用，无限记录主要检查数组长度；缺少完整字段、题面和历史验证。
6. **保存失败静默处理**：用户看不到进度未保存的状态。
7. **动态设置失效、音频字段空置**：设置模型与实际能力不一致。
8. **持续全屏重绘**：即使界面静止也每帧重建背景、阴影和命中列表。
9. **绘制过程中修改状态**：绘制猫时调用 `ensureCatFaces()`，混合渲染和存档修复职责。
10. **集成测试不足**：现有手势测试没有覆盖真实页面分发、遮罩、多指和越界松手。
11. **文档与代码漂移**：旧主题说明、GIF 路径、帧率注释及部分玩法承诺与实现不符。

## 9. 哪些现有代码应该保留

建议保留这些实现，并在原基础上加固：

- [env.ts](/D:/Github/Purrdoku-wx/src/platform/env.ts)：微信 Canvas、DPR、安全区和尺寸适配。
- `engine/structure.ts`、`search.ts`：区域连通、合法解和独立唯一性验证。
- `engine/logic.ts`、`hint.ts`：B1–B4、C1 推理及明确标注的纠错／揭示。
- `engine/board.ts`：标记、放猫、自动排除等纯函数。
- `game/play.ts`：历史快照、累计统计、提示应用。
- `engine/rng.ts`、`signature.ts`：可复现随机数、旋转／镜像／换色去重。
- `game/gestures.ts`：独立、可测试的手势状态机结构。
- `game/storage.ts`：存储接口隔离方式。
- `ui/theme.ts`：已经对齐 H5 的主题参数。
- 图片加载缓存和加载失败后的绘图回退。
- `onHide/onShow` 中暂停计时、清理手势、恢复尺寸的基本机制。

不需要因为代码此前由 Cursor 编写就整体替换；应按行为正确性和维护成本判断。

## 10. 哪些部分建议重构

| 范围     | 建议                                       |
| -------- | ------------------------------------------ |
| 页面输入 | 独立触点跟踪、按钮按压、弹窗命中层         |
| `app.ts` | 逐步拆成页面绘制、模式控制、结算和存档协调 |
| 拖动模型 | 保存起始快照和累计路径，整次拖动只提交一次 |
| 动画     | 增加统一时钟、暂停和开关控制               |
| 出题     | 建立可取消的异步任务与一道预备题队列       |
| 出题接收 | 合格题准备好后，再更新关号和当前题         |
| 备用题   | 按尺寸、分数、保护规则和近期签名筛选       |
| 存档     | 增加校验、迁移、失败提示和每日会话日期     |
| 提示绘制 | 用“应用后的预览棋盘”展示完整变化           |
| 资源加载 | 明确本地文件与内嵌资源的选择，减少重复     |
| 反馈     | 统一管理声音、振动、正确／错误和通关效果   |

微信 Worker 或分帧任务的具体选择，应在实施时结合运行环境验证；H5 的 Web Worker 代码不能直接搬用。

## 11. 改造优先级

| 优先级 | 内容                                                                            | 原因                           |
| ------ | ------------------------------------------------------------------------------- | ------------------------------ |
| **P0** | 弹窗穿透、越界松手、多指串扰、跨日存档、完成后重开状态                          | 可能误操作、卡住输入或写错进度 |
| **P1** | 拖动路径一致性、减少同步保存、动态开关、受约束出题兜底、异步出题、节奏历史      | 直接影响主要玩法和操作体验     |
| **P2** | 棋盘比例、格子／X／猫视觉、规则示意、提示预览、页面和胜利弹窗、音效音乐、纪念关 | 拉齐 H5 的产品完成度           |
| **P3** | 静态绘制缓存、精灵图压缩、旧资源清理、文档整理、广泛机型调优                    | 降低后续维护和运行成本         |

真机检查应从输入阶段开始，P3 是扩大覆盖和最终调优，不能等全部做完才第一次验证。

## 12. 推荐实施顺序与阶段验收

| 阶段                         | 工作范围                                           | 验收要求                                                 |
| ---------------------------- | -------------------------------------------------- | -------------------------------------------------------- |
| **第一阶段：正确性修复**     | 弹窗隔离、完整触摸收尾、多指、每日日期、完成后重开 | 不穿透、不残留手势、不串日期、不漏结算；基础规则回归通过 |
| **第二阶段：输入手感**       | 累计拖动路径、最终坐标处理、按压反馈、保存节流     | 单击／双击不重复执行；转弯拖动正确；一次撤销恢复整次拖动 |
| **第三阶段：出题稳定性**     | 严格备用池、异步准备、失败重试、节奏用途和版本记录 | 失败保留当前进度；不跳关；不突破尺寸／难度／去重约束     |
| **第四阶段：棋盘表现**       | 格间距、圆角、X、猫、放置和错误动画                | 与 H5 同尺寸对照；动态关闭真正静止；弹窗期间暂停待机     |
| **第五阶段：页面与反馈**     | 首页、规则示意、提示、设置、结算、音频、纪念关     | 提示预览与应用一致；各模式设置一致；完成流程完整         |
| **第六阶段：性能和设备验收** | 资源去重、缓存、包体、安全区、不同机型测试         | 6×6–9×9、后台恢复和长时间游玩稳定                        |

每阶段作为独立改动范围，完成相关测试和设备检查后再推进；开发时使用清晰的功能分支，不自动提交或推送。

**建议确认后先做第一阶段，范围限定为输入与存档正确性修复。** 本轮按 [Prompt.md](/D:/Github/Purrdoku-wx/Prompt.md) 要求停在分析和方案阶段，等待你确认后再开发。
