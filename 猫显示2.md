> 请把 Purrdoku 微信小游戏棋盘上的猫咪动画，从当前 **4 帧 Sprite Sheet** 升级为新的 **8 帧 Sprite Sheet**。
>
> ## 现状
>
> 当前已经实现：
>
> - 棋盘上有多种猫咪；
> - 每次放猫会随机选择一种猫；
> - 猫咪类型会绑定到对应格子，不会因为重绘重新随机；
> - 四帧 Sprite 动画已经可以正常播放；
> - 放猫、取消、Undo、Restart、Hint Apply 等现有逻辑已经正常。
>
> **这些逻辑全部保留，不要重构。**
>
> 本次需求只是：
>
> **把原来的 4 帧猫咪动画素材替换为新的 8 帧猫咪动画素材，让动作更加流畅。**
>
> ---
>
> ## 新素材位置
>
> 新的 8 帧猫咪 Sprite Sheet 已经准备好，位于：
>
> `assets/cats8/`
>
> 不需要生成、修改或重新绘制图片。
>
> 请先检查该目录：
>
> - 实际文件数量；
> - 文件名；
> - 图片尺寸；
> - Sprite Sheet 排列方式；
> - 每张图是否都是 8 帧；
> - 12 种猫与原来 `assets/cats/` 中猫咪素材的对应关系。
>
> **不要凭文件名或布局猜测，先检查实际素材。**
>
> ---
>
> ## Sprite Sheet 布局
>
> 如果实际素材与预期一致，为：
>
> ```text
> Frame 1 | Frame 2 | Frame 3 | Frame 4
> Frame 5 | Frame 6 | Frame 7 | Frame 8
> ```
>
> 即：
>
> **4 列 × 2 行，共 8 帧**
>
> 播放顺序必须按照：
>
> `1 → 2 → 3 → 4 → 5 → 6 → 7 → 8 → 1`
>
> 但仍然以实际图片检查结果为准。
>
> ---
>
> ## 核心修改
>
> 当前代码如果类似：
>
> `FRAME_COUNT = 4`
>
> 或者当前 Sprite Sheet 是按照 2×2 / 4 帧进行 source rect 计算，请修改为支持新的：
>
> `4 columns × 2 rows = 8 frames`
>
> 根据 `frameIndex` 正确计算：
>
> - `sourceX`
> - `sourceY`
> - `sourceWidth`
> - `sourceHeight`
>
> 例如概念上：
>
> `column = frameIndex % 4`
>
> `row = Math.floor(frameIndex / 4)`
>
> 然后从 Sprite Sheet 中截取对应帧。
>
> 具体代码请按照项目当前动画实现修改，不要为了照搬这个公式重写动画系统。
>
> ---
>
> ## 播放速度
>
> 4 帧升级成 8 帧以后，**不要继续机械沿用原来每帧停留时间**，否则完整动作可能变成原来的两倍慢。
>
> 目标是：
>
> **8 帧比原来的 4 帧动作更流畅，但整体动作节奏不要明显变慢。**
>
> 请先检查当前 4 帧动画完整一轮的实际时长，然后让新的 8 帧动画完整一轮保持接近原来的动作周期。
>
> 例如：
>
> 如果原来：
>
> `4 × 200ms ≈ 800ms`
>
> 那么新的 8 帧可以大约：
>
> `8 × 100ms ≈ 800ms`
>
> 这里只是示例。
>
> **以当前项目实际动画周期为准，不要直接写死 100ms。**
>
> ---
>
> ## 不要改随机猫逻辑
>
> 当前已经实现的：
>
> `放猫 → 随机选择一种猫 → 绑定到当前格子`
>
> 继续保持。
>
> 只是原来每个 `catVariant` 对应：
>
> `assets/cats/` 中的一张 4 帧图
>
> 现在改为对应：
>
> `assets/cats8/` 中的一张 8 帧图。
>
> 不要重新设计随机算法。
>
> 不要在 render / draw 时重新随机。
>
> ---
>
> ## 不要修改游戏逻辑
>
> 8 帧升级只是视觉表现升级。
>
> 不允许影响：
>
> - 棋盘规则
> - 放猫逻辑
> - 打 ×
> - 双击
> - Hint Solver
> - Hint Apply
> - Undo
> - Restart
> - 胜负判断
> - 关卡
> - 无限模式
>
> `catVariant` 仍然只是视觉属性。
>
> ---
>
> ## 尺寸必须保持一致
>
> 新的 8 帧猫在棋盘格里的**最终视觉尺寸、居中位置和裁剪范围应尽量与现在的 4 帧版本一致**。
>
> 不要因为 Sprite Sheet 从 4 帧变成 8 帧导致：
>
> - 猫突然变大/变小；
> - 猫在格子里跳位置；
> - 不同帧中心点漂移；
> - 超出格子；
> - 拉伸变形。
>
> 每一帧都应该绘制到现有猫咪的同一个 destination rect 中。
>
> ---
>
> ## 注意帧间抖动
>
> 接入后重点观察：
>
> **播放 1→8 帧过程中猫头主体是否发生明显左右/上下抖动。**
>
> 如果 Sprite Sheet 每格尺寸完全一致，则按照固定单元格裁剪，不要根据每帧实际非透明内容重新计算尺寸或中心。
>
> 否则很容易造成动画播放时猫咪整体位置漂移。
>
> ---
>
> ## 循环衔接
>
> 新动画按照：
>
> `1 → 2 → 3 → 4 → 5 → 6 → 7 → 8 → 1`
>
> 循环。
>
> 素材本身已经考虑循环动作。
>
> 不需要额外生成中间帧，也不要在 `8 → 1` 之间通过代码插值生成图片。
>
> ---
>
> ## 性能
>
> 沿用现有图片加载和动画机制。
>
> 不要因为从 4 帧升级成 8 帧：
>
> - 给每只猫创建新的独立 timer；
> - 每帧重新加载 Sprite Sheet；
> - 每帧创建 Image；
> - 引入新的动画库；
> - 大规模重构 Canvas 渲染。
>
> **现有动画架构能用就继续用。**
>
> ---
>
> ## 旧素材
>
> 暂时不要删除：
>
> `assets/cats/`
>
> 先让新的：
>
> `assets/cats8/`
>
> 正常工作。
>
> 等我确认新 8 帧动画没有问题以后，再决定是否删除旧 4 帧素材。
>
> ---
>
> ## 修改原则
>
> 本次是一个**最小范围升级**：
>
> `4帧猫素材`
>
> ↓
>
> `8帧猫素材`
>
> 而不是：
>
> `重新实现猫咪动画系统`
>
> 修改前先检查当前猫咪动画代码和 `assets/cats8/` 的实际素材，确认需要修改哪些地方。
>
> 完成后告诉我：
>
> 1. 修改了哪些文件；
> 2. 8 帧是如何切帧的；
> 3. 当前每帧播放时间是多少；
> 4. 完整 8 帧循环是多少毫秒；
> 5. 是否保留了原来的随机猫逻辑；
> 6. 是否发现某些素材存在帧尺寸/排列异常。
>
> **不要自动 commit，不要 push。**
