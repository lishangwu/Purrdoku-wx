请修改 Purrdoku 微信小游戏棋盘上的猫咪显示逻辑。

## 现状

当前棋盘上的猫咪四帧动画已经实现，并且：

- Sprite Sheet 切帧正常
- 四帧播放正常
- 播放速度正常
- 循环方式正常
- 猫咪在格子中的大小和位置正常
- 当前整体动画效果是可以接受的

**本次不要重构现有猫咪动画系统，不要修改播放速度、切帧方式、循环方式、尺寸和位置。**

这次只是新增了更多猫咪素材。

---

## 新素材

我已经准备好了 **12 只不同猫咪的四帧图**，放在：

`assets/cats/`

这些图片已经做好，不需要生成、不需要修改、不需要重新切图。

请先检查 `assets/cats/` 目录中的实际文件名和数量。

每个文件都是：

**一只猫的一套四帧动画素材。**

---

## 核心需求

原来棋盘上的猫咪可能一直使用同一套四帧素材。

现在改成：

**每次棋盘上成功放置一只猫时，从 `assets/cats/` 中的 12 套猫咪素材里随机选择 1 套。**

例如：

```text
第一次放猫 → 随机猫 A
第二次放猫 → 随机猫 F
第三次放猫 → 随机猫 C
第四次放猫 → 随机猫 K
```

然后继续使用现有四帧动画逻辑播放这只猫。

---

## 非常重要：随机只发生一次

不要在每次 render / draw / animation frame 时重新随机猫咪。

正确逻辑：

```text
玩家放猫
↓
随机选一个 catVariant
↓
把 catVariant 绑定到这个格子/这只猫
↓
后续所有动画帧都继续使用这个 catVariant
```

也就是说：

如果某个格子第一次随机到的是 `cat-07`，

那么只要这只猫还在这个格子里：

**它就一直是 cat-07。**

不能因为：

- 棋盘重绘
- 动画更新
- 其他格子变化
- 页面刷新状态
- Hint 更新

而重新换成别的猫。

---

## 尽量复用现有逻辑

本次优先采用**最小改动**。

请找到当前代码中：

- 固定猫咪图片资源的位置
- 当前猫咪 Sprite Sheet 的加载逻辑
- 当前四帧动画播放逻辑
- 当前放猫逻辑
- 当前棋盘 Cell 数据结构

然后把原来的：

```text
固定使用某一张猫咪四帧图
```

改成：

```text
放猫时随机选择 12 张中的一张
```

现有动画播放器继续复用。

不要为了新增 12 种猫去重新实现一套动画系统。

---

## Cat Variant

建议给每只已经放置的猫保存一个视觉属性，例如：

```ts
catVariant;
```

它只负责决定：

**这只猫使用哪一套 Sprite Sheet。**

例如：

```ts
{
  hasCat: true,
  catVariant: 6
}
```

具体数据结构根据当前项目现有代码设计，不要求照抄。

---

## 游戏逻辑不能受影响

`catVariant` 只是视觉属性。

12 种猫在游戏规则里仍然全部都是：

```text
CAT
```

不要让猫咪种类参与：

- 行规则
- 列规则
- 区域规则
- 相邻规则
- 胜负判断
- Hint Solver
- 关卡答案
- 无限模式算法

12 种猫只是长得不同、动作不同。

**游戏逻辑完全一样。**

---

## 撤销 / 删除 / 重开

请检查现有：

- 取消猫
- Undo
- Restart
- Reset
- Hint Apply
- 关卡重开

逻辑。

要求：

### 猫被删除

对应的 `catVariant` 一起清除。

### Undo

如果 Undo 是恢复之前的棋盘状态：

应该恢复原来的猫咪类型。

例如：

```text
原来这个格子是 cat-05
↓
撤销
↓
再次恢复时仍然是 cat-05
```

不要 Undo 后重新随机。

### Restart / Reset

按照当前项目原有重置逻辑处理即可。

---

## Hint 放猫

当前提示系统如果最终结论是：

`把猫放这里`

玩家点击应用以后：

也应该使用和正常玩家放猫相同的逻辑：

```text
placeCat()
↓
随机 catVariant
↓
使用现有四帧动画播放
```

不要让 Hint 单独写另一套猫咪创建逻辑。

尽量保证：

- 玩家手动放猫
- Hint 应用放猫
- 其他合法放猫入口

最终走同一套猫咪创建逻辑。

---

## 随机规则

默认允许重复。

例如：

```text
cat-03
cat-08
cat-03
cat-11
```

这是正常的。

不需要做复杂的“12 只猫用完一轮才能重复”算法。

只需要自然随机即可。

如果当前实现非常方便，可以尽量避免连续两个新放置的猫使用完全相同的素材，但这不是强制要求。

**不要为了防重复增加复杂逻辑。**

---

## 资源加载

不要每次放猫时重新从磁盘加载图片。

沿用当前项目已有图片缓存/资源加载机制。

12 套猫素材应该正常缓存，然后猫咪实例只记录自己使用哪一套资源。

不要：

- 每帧重新创建图片
- 每帧重新加载资源
- 因为随机猫而增加大量 timer

现有动画系统怎么播放，就继续怎么播放。

---

## 本次明确不要做的事情

本次不要：

- 修改现有四帧播放速度
- 修改现有帧切换方式
- 修改现有猫咪大小
- 修改现有猫咪位置
- 修改现有循环节奏
- 修改现有 Sprite 动画实现
- 重构整个棋盘动画系统
- 修改游戏规则
- 修改 Hint Solver
- 重新制作图片

**只做“多猫随机素材接入”。**

---

## 实施前先检查

修改前先确认：

1. `assets/cats/` 实际有哪些文件
2. 当前固定猫咪资源在哪里配置
3. 当前 Sprite 四帧动画怎么播放
4. 放猫逻辑在哪里
5. Cell / Board State 如何保存猫状态
6. Undo 是否保存完整棋盘状态
7. Hint 应用猫是否走统一放猫逻辑

然后给我一个简短实施方案。

如果现有动画系统可以直接复用，就不要动它。

---

## 最终效果

最终效果应该是：

玩家每次在棋盘上放一只猫：

```text
随机选择 12 种猫中的一种
↓
在这个格子里使用该猫现有的四帧动画
↓
猫咪类型保持固定
```

这样棋盘上可以同时出现不同的猫：

```text
A猫   ×   F猫
   C猫
×       J猫
H猫   ×
```

但它们的游戏规则完全一样。

最终目标：

**保留现在已经正常工作的四帧动画机制，只让棋盘上的猫咪从单一素材升级为 12 种随机素材。**
